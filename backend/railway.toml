[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd /app && PYTHONPATH=/app alembic upgrade head || true; uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
